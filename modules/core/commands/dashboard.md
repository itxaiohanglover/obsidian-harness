Run the following command and show the output directly to the user, no additional processing needed:

```bash
npx obsidian-harness dashboard
```

If the command fails, fall back to manual vault scanning.
